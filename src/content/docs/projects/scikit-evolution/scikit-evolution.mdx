---
title: 'scikit-evolution'
description: 'A Python evolutionary hyperparameter optimization framework for scikit-learn-style estimators, using Artificial Immune System search over model families, parameter spaces, and cross-validation scores.'
category: 'projects'
tags: ['Python', 'scikit-learn', 'NumPy', 'TensorFlow', 'skflow', 'lightning', 'MySQL', 'Evolutionary Algorithms']
order: 6
role: 'Author'
dates: '2016'
roleSummary: 'I built the shell learner abstraction, typed hyperparameter system, Artificial Immune System optimizer, experiment infrastructure, persistence paths, and analysis artifacts.'
metrics:
  - '87 learner shell classes across regression, classification, and clustering model families'
  - '15 parameter enum types for bounded, categorical, gaussian, dynamic, constrained, and unconstrained hyperparameters'
  - '139 run-output files and 51 analysis artifacts from evolutionary model-search experiments'
decisions:
  - title: 'Shell learners wrap heterogeneous estimators'
    detail: 'Each supported estimator is adapted into a common shell so optimizers can search across model families instead of a single algorithm.'
    tradeoff: 'Wrapping many libraries creates maintenance work around inconsistent estimator behavior.'
  - title: 'Typed hyperparameters drive mutation'
    detail: 'The `EParameter` vocabulary gives integer, real, categorical, gaussian, constrained, unconstrained, and kernel settings shared randomization and mutation rules.'
    tradeoff: 'A richer parameter model is more complex than simple grid definitions.'
  - title: 'AIS as the primary search path'
    detail: 'The main optimizer creates candidate shell learner solutions, scores them with cross-validation, mutates them with rank-influenced intensity, and keeps strong candidates across generations.'
    tradeoff: 'Evolutionary search is slower and less predictable than direct model tuning.'
  - title: 'Preserve experiment outputs'
    detail: 'Run outputs, analysis artifacts, SQL scripts, and tests capture the state of real experiments rather than only the framework code.'
    tradeoff: 'The repository is heavier, but the portfolio page can point to concrete evidence of use.'
outcome:
  - 'The framework can search model families and hyperparameter spaces across regression, classification, and clustering experiments.'
  - 'Experiment records include 139 run-output files, 51 analysis artifacts, and SQL scripts for querying best learners and simulation results.'
  - 'The project demonstrates early hands-on work with AutoML-style search, model abstraction, and evolutionary optimization.'
lessons: 'AutoML-style systems need two abstractions to be useful: a model wrapper that normalizes estimator behavior and a parameter vocabulary that mutation can understand. The archival lesson is that search machinery is only persuasive when experiment outputs survive beside the code.'
---

## Context

scikit-evolution is a Python framework for treating machine-learning model configuration as an optimization problem. It wraps scikit-learn-style estimators in "shell learner" adapters, defines their searchable hyperparameter spaces, and uses evolutionary search to discover model and parameter combinations that perform well on a given dataset.

## The Problem

Trying many model families by hand is repetitive and inconsistent. Each estimator exposes different parameters, defaults, constraints, and scoring behavior. This project explored whether a shared learner abstraction and evolutionary search loop could make cross-family model selection more systematic.

## Constraints

- **Estimator variety.** Linear models, trees, ensembles, SVMs, nearest-neighbour methods, neural networks, Gaussian processes, TensorFlow/skflow wrappers, and lightning wrappers all behave differently.
- **Parameter shape.** Search needs to mutate integers, floats, strings, booleans, arrays, maps, bounded values, constrained values, and kernels through one vocabulary.
- **Evaluation cost.** Candidate models are scored through cross-validation, so every generation has a real runtime cost.
- **Experiment traceability.** Search results, SQL queries, output files, tests, and analysis artifacts need to survive beyond the run.
- **Archival dependencies.** The project reflects the ML library landscape and research code style of 2016.

## The Design

The main search path is an Artificial Immune System optimizer. It creates a population of candidate `ShellLearnerSolution` objects, scores each candidate with scikit-learn `cross_val_score`, sorts by fitness, mutates candidates with rank-influenced intensity, avoids duplicate configurations, and keeps the best-performing candidates across generations. The optimizer layer also includes hill climbing, random search, and modules for genetic algorithms, simulated annealing, particle swarm optimization, tabu search, and evolution strategies.

The learner abstraction is the core of the project. The repository contains 87 learner shell classes across regression, classification, and clustering model families, including linear models, trees, ensembles, SVMs, nearest-neighbour methods, neural networks, Gaussian processes, kernel ridge models, naive Bayes, TensorFlow/skflow wrappers, and lightning wrappers. Helpers group those learners by problem type and data-size suitability so experiments can search a selected pool rather than a single estimator.

### Hyperparameter Vocabulary

The hyperparameter system gives each shell a common vocabulary for randomization and mutation. The `EParameter` enum defines 15 parameter categories, including integer, real, string, boolean, array, map, category, bounded, mixed, dynamic, gaussian, Dirichlet, constrained, unconstrained, and kernel values. Bounds, movement rules, randomizers, and mutators sit behind that vocabulary so the optimizer can mutate very different model settings through one interface.

### Experiment Infrastructure

The framework also includes experiment infrastructure around the search loop. It supports custom scoring such as MAPE, regression and classification problem scripts, domain-specific normalizers for load-forecasting style work, MySQL query builders and schema scripts for persisted results, file-output fallbacks, and a timeout path for evaluations that might hang.

The repository preserves evidence of real experimentation: 139 run-output files, 51 analysis artifacts, SQL scripts for querying best learners and simulation results, and 19 test files covering mutation, randomization, parameter bounds, metrics, classifiers, regressors, data handling, preprocessing, SQL queries, GPU paths, and database connectivity.

The project is intentionally archival. As a portfolio project, it shows early hands-on work with AutoML-style search, model abstraction, hyperparameter typing, evolutionary algorithms, experiment persistence, and the practical messiness of making many heterogeneous machine-learning libraries behave through one optimizer-facing contract.

The source is being prepared for public GitHub release. Once it is public, this project page can link directly to the repository from the project card.
