---
interface Props {
  tone?: 'neutral' | 'info' | 'success' | 'warning' | 'danger';
}

const { tone = 'neutral' } = Astro.props;
---

<span class:list={['docs-badge not-prose', `docs-badge-${tone}`]}>
  <slot />
</span>
